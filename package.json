{
  "name": "connect-four-game",
  "version": "1.0.0",
  "description": "Connect Four game with automated testing using Selenium in Azure Pipelines.",
  "main": "connect_four.js",

  "scripts": {
    "start": "node connect_four.js",
    "test": "mocha --reporter mocha-junit-reporter --reporter-options mochaFile=TestResults/Test1.xml seleniumTest.js",
    "build": "echo 'No build step defined'"
  },

  "dependencies": {
    "selenium-webdriver": "^4.0.0",
    "chromedriver": "^114.0.0",
    "mocha": "^10.0.0"
  },

  "devDependencies": {
    "mocha-junit-reporter": "^2.0.0"  // Use a valid and supported reporter like mocha-junit-reporter
  },

  "author": "Your Name",
  "license": "ISC"
}
